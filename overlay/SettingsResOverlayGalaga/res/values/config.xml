<?xml version='1.0' encoding='UTF-8'?>
<!--
  Copyright (C) 2023 The LineageOS Project

  SPDX-License-Identifier: Apache-2.0
-->

<!-- These resources are around just to allow their values to be customized
     for different hardware and product builds.  Do not translate.

     NOTE: The naming convention is "config_camelCaseValue". Some legacy
     entries do not follow the convention, but all new entries should. -->
<resources xmlns:xliff="urn:oasis:names:tc:xliff:document:1.2">
    <!-- Whether to show Smooth Display feature in Settings Options -->
    <bool name="config_show_smooth_display">true</bool> 

    <!-- Show battery cycle count -->
    <bool name="config_show_battery_cycle_count" translatable="false">true</bool>

    <!-- Device marketname -->
    <string name="def_device_name_simple">CMF by Nothing Phone 2 Pro</string>

    <!-- The radius of the enrollment progress bar, in DP. -->
    <integer name="config_udfpsEnrollProgressBar" translatable="false">92</integer>

    <!-- Display settings screen, Color mode options. Must be the same length and order as
         config_color_mode_options_values below. Only the values that also appear in
         frameworks/base/core/res/res/values/config.xml's config_availableColorModes are shown. -->
    <string-array translatable="false" name="config_color_mode_options_strings">
        <item>Alive</item>
        <item>Standard</item>
    </string-array>

    <!-- Display settings screen, Color mode options. Must be the same length and order as
         config_color_mode_options_strings above. Only the values that also appear in
         frameworks/base/core/res/res/values/config.xml's config_availableColorModes are shown. -->
    <integer-array translatable="false" name="config_color_mode_options_values">
        <item>260</item>
        <item>0</item>
    </integer-array>
</resources>
